# 现代占星计算规则

这份文档给“世界知识不足、但会写代码”的实现型 agent 使用。

目标不是解释占星意义，而是明确：

- 输入是什么
- 怎么计算
- 哪些是通行定义
- 哪些不是唯一标准，必须按本项目规则实现
- 输出字段应该长什么样
- 测试该怎么写

本文件只覆盖现代占星计算器的核心模块：

1. `Synastry`
2. `Composite`
3. `Davison`
4. `Secondary Progressions`
5. `Solar Arc Directions`
6. `Pattern Detection`

## 1. 公共规则

### 1.1 基础输入

所有现代模块都基于现有项目已使用的这类输入：

- `moment`
  - `year`
  - `month`
  - `day`
  - `hour`
  - `minute`
  - `timezone`
- `birth settings`
  - `latitude`
  - `longitude`
  - `houseSystem`
  - `zodiac`

现代模块统一新增这些参数：

- `nodeMode`
  - `true_node`
  - `mean_node`
- `extraPoints`
  - 可包含：`CHIRON`, `MEAN_LILITH`, `OSCU_LILITH`, `VERTEX`
- `includeOuterPlanets`
  - 默认 `true`
- `aspects`
  - 用户选中的相位列表
- `aspectOrb`
  - 全局默认 orb

### 1.2 时间与黄经

全部模块复用现有底层规则：

1. 本地时间先转带时区 `datetime`
2. 再转 UTC
3. 再转 Swiss Ephemeris `Julian Day`
4. 所有黄经统一做 `norm360()` 归一化到 `[0, 360)`

已有 helper 应优先复用：

- `moment_to_local_datetime()`
- `jd_from_datetime()`
- `moment_to_jd()`
- `norm360()`
- `angular_separation()`

### 1.3 点位集

现代默认主点位集：

- `SUN`
- `MOON`
- `MERCURY`
- `VENUS`
- `MARS`
- `JUPITER`
- `SATURN`
- `URANUS`
- `NEPTUNE`
- `PLUTO`

可选点位：

- `CHIRON`
- `TRUE_NODE` 或 `MEAN_NODE`
- `SOUTH_TRUE_NODE` 或 `SOUTH_MEAN_NODE`
- `MEAN_LILITH`
- `OSCU_LILITH`
- `VERTEX`

### 1.4 节点规则

节点规则固定，不允许 agent 自行发明别的口径：

- 若 `nodeMode == "true_node"`，只输出：
  - `TRUE_NODE`
  - `SOUTH_TRUE_NODE`
- 若 `nodeMode == "mean_node"`，只输出：
  - `MEAN_NODE`
  - `SOUTH_MEAN_NODE`

南交点不单独调天文库，统一用北交点黄经 `+180°` 后再 `norm360()`。

### 1.5 Lilith 规则

Lilith 口径固定：

- `MEAN_LILITH` 对应 Swiss Ephemeris `MEAN_APOG`
- `OSCU_LILITH` 对应 Swiss Ephemeris `OSCU_APOG`

不要新增第三种 Lilith。

### 1.6 Vertex 规则

Vertex 不是普通行星：

- 不走 `BODY_REGISTRY` 普通天体分支
- 来自 house / angles 计算流程
- 输出时当作虚点

### 1.7 相位规则

支持的相位与角度：

| id | angle |
|---|---:|
| `conjunction` | 0 |
| `opposition` | 180 |
| `trine` | 120 |
| `square` | 90 |
| `sextile` | 60 |
| `quincunx` | 150 |
| `semisextile` | 30 |
| `semisquare` | 45 |
| `sesquisquare` | 135 |
| `quintile` | 72 |
| `biquintile` | 144 |

命中规则固定：

1. `separation = angular_separation(lon_a, lon_b)`
2. `orb = abs(separation - aspect_angle)`
3. 若 `orb <= aspect.orb` 则命中

排序规则：

1. 先按 `orb` 升序
2. 再按名称做稳定排序

默认 orb 约定：

- major aspects：`6°`
- 日月参与的 major aspects：可放宽到 `8°`
- minor aspects：`2°` 到 `3°`
- 若请求里显式传了 `aspect.orb`，以请求值为准

### 1.8 宫位与角点

所有新增现代模式：

- 宫位计算必须复用现有 house builder
- `ASC / MC / DSC / IC` 命名与现有项目一致
- `DSC = ASC + 180°`
- `IC = MC + 180°`

### 1.9 Circular Midpoint

新增统一 helper：

`circular_midpoint(lon1, lon2) -> float`

规则：

1. 沿两点之间的最短弧取中点
2. 实现公式：

```python
diff = ((lon2 - lon1 + 540.0) % 360.0) - 180.0
mid = norm360(lon1 + diff / 2.0)
```

如果两点刚好相差 `180°`：

- 这在几何上不唯一
- 为了 deterministic，项目固定返回：

```python
norm360(lon1 + 90.0)
```

这条规则必须写测试。

## 2. Synastry

### 2.1 定义

Synastry 是两张本命盘的比较，不生成第三张“关系盘”。

### 2.2 输入

- `personA`
- `personB`
- `houseSystem`
- `zodiac`
- `nodeMode`
- `extraPoints`
- `aspects`
- `aspectOrb`

### 2.3 计算步骤

1. 分别计算 A、B 两张本命盘：
   - 行星/点位黄经
   - 角点
   - 宫位
2. 计算 A 与 B 的跨盘相位：
   - `for point in A_points`
   - `for point in B_points`
   - 逐个检查所选相位
3. 计算落宫：
   - A 的每个点位落入 B 的哪一宫
   - B 的每个点位落入 A 的哪一宫
4. 可选保留 A/B 各自本盘相位，但不能混进跨盘相位表

### 2.4 输出规则

必须分开返回：

- `person_a_planets`
- `person_b_planets`
- `person_a_angles`
- `person_b_angles`
- `person_a_houses`
- `person_b_houses`
- `cross_aspects`
- `a_in_b_houses`
- `b_in_a_houses`
- `warnings`

`cross_aspects` 每一行至少要有：

- `point_a_id`
- `point_a_name`
- `point_b_id`
- `point_b_name`
- `aspect_id`
- `aspect_name`
- `angle`
- `separation`
- `orb`

### 2.5 禁止事项

- 不生成新盘
- 不把 A 本盘相位和跨盘相位混在一起
- 不输出解释文字

## 3. Composite

### 3.1 定义

Composite 使用 midpoint composite 规则。它是“点位中点盘”，不是时间中点盘。

### 3.2 输入

与 `Synastry` 相同。

### 3.3 计算步骤

1. 先分别算出 A/B 的本命点位
2. 对所有参与 composite 的点位取 `circular_midpoint()`：
   - 十行星
   - 选中的节点
   - 选中的 Chiron / Lilith
   - `ASC`
   - `MC`
3. `DSC = ASC + 180°`
4. `IC = MC + 180°`
5. 用 composite 的 `ASC / MC` 重新建宫
6. 计算 composite 点位之间的相位
7. 跑 pattern detection

### 3.4 宫位规则

项目规则固定：

- 先算 `ASC` 和 `MC` 的中点
- 再由这组角点重建 houses
- 不采用“逐个宫头取中点”的另一套做法

### 3.5 输出

- `meta`
- `angles`
- `houses`
- `planets`
- `aspects`
- `patterns`
- `warnings`

`meta.method` 固定写：

- `composite_midpoint`

## 4. Davison

### 4.1 定义

Davison 是“真实时间空间中点盘”，不是逐点中点盘。

### 4.2 输入

与 `Synastry` 相同。

### 4.3 计算步骤

1. 取 A/B 的出生 UTC datetime
2. 计算时间中点：

```python
mid_dt = dt1 + (dt2 - dt1) / 2
```

3. 计算地点中点：

```python
mid_lat = (lat1 + lat2) / 2.0
mid_lon = (lon1 + lon2) / 2.0
```

4. 用 `mid_dt + mid_lat + mid_lon` 直接计算一张真实星盘
5. 计算相位
6. 跑 pattern detection

### 4.4 地点规则

项目 v1 固定：

- 使用经纬度算术平均
- 不做大圆中点

### 4.5 输出

- `meta`
- `angles`
- `houses`
- `planets`
- `aspects`
- `patterns`
- `warnings`

`meta.method` 固定写：

- `davison_midtime_midspace`

## 5. Secondary Progressions

### 5.1 定义

采用 standard secondary progression：

- `1 day after birth = 1 year of life`

### 5.2 输入

- `birth`
- `reference`
- `houseSystem`
- `zodiac`
- `nodeMode`
- `extraPoints`
- `aspects`

### 5.3 年龄计算

先算 UTC 下的真实年龄：

```python
age_years = (reference_utc - birth_utc).total_seconds() / (365.2422 * 86400.0)
```

然后把年龄直接换成 progression offset：

```python
offset_days = age_years
```

### 5.4 Progressed Datetime

```python
progressed_dt = birth_utc + timedelta(days=offset_days)
```

### 5.5 Progressed Planets

用 `progressed_dt` 直接计算 progressed planets。

### 5.6 Progressed Angles / Houses

项目 v1 规则：

- 用 `progressed_dt`
- 在原出生地点
- 按原 `houseSystem`
- 重新建角点和宫位

这等价于工程上采用 progressed sidereal time 来重算角宫。

### 5.7 相位输出

必须分别计算：

- `progressed_to_natal_aspects`
- `progressed_to_progressed_aspects`

默认主输出是：

- `progressed_to_natal_aspects`

### 5.8 Progressed Lunation

至少输出：

- `sun_moon_separation`
- `phase_angle`
- `phase_name`

计算规则：

1. `sep = angular_separation(progressed_sun, progressed_moon)`
2. 找到最接近的主月相角度：
   - `0`
   - `45`
   - `90`
   - `135`
   - `180`
   - `225`
   - `270`
   - `315`
3. 输出最近角度与 phase 名称

### 5.9 时间不准警告

如果出生时间不准或缺失：

- 行星推进仍可算
- 但必须在 `warnings` 里提示：
  - progressed angles / houses may be unreliable without accurate birth time

### 5.10 输出

- `meta`
- `natal_planets`
- `progressed_planets`
- `natal_angles`
- `progressed_angles`
- `natal_houses`
- `progressed_houses`
- `progressed_to_natal_aspects`
- `progressed_to_progressed_aspects`
- `progressed_lunation`
- `warnings`

## 6. Solar Arc Directions

### 6.1 定义

采用 true solar arc。

### 6.2 输入

与 `Secondary Progressions` 相同。

### 6.3 计算步骤

1. 先按次限推进算出 `progressed_sun_lon`
2. 取本命太阳黄经 `natal_sun_lon`
3. 计算 solar arc：

```python
arc = norm360(progressed_sun_lon - natal_sun_lon)
```

4. 对所有本命行星整体平移：

```python
directed_lon = norm360(natal_lon + arc)
```

5. 对角点整体平移：

```python
sa_asc = norm360(natal_asc + arc)
sa_mc = norm360(natal_mc + arc)
sa_dsc = norm360(sa_asc + 180.0)
sa_ic = norm360(sa_mc + 180.0)
```

6. 宫头项目 v1 简化规则：
   - 所有 natal cusps 全部 `+ arc`
   - 不做另一套独立 solar arc house system
7. 计算 `solar_arc_to_natal_aspects`

### 6.4 不做的内容

v1 不实现：

- converse solar arc
- Naibod key
- 多种 solar arc key 切换

### 6.5 输出

- `meta`
- `natal_planets`
- `solar_arc_planets`
- `solar_arc_angles`
- `solar_arc_houses`
- `solar_arc_to_natal_aspects`
- `arc_value`
- `warnings`

## 7. Pattern Detection

### 7.1 说明

Pattern detection 没有行业唯一标准。

下面这些规则全部视为“本项目规则”，实现必须严格按这里，不允许 agent 自己猜。

### 7.2 输入

- 一组点位黄经
- 一组命中的相位
- 可选宫位信息

### 7.3 参与点位

默认纳入 pattern detection 的主要点位：

- `SUN`
- `MOON`
- `MERCURY`
- `VENUS`
- `MARS`
- `JUPITER`
- `SATURN`
- `URANUS`
- `NEPTUNE`
- `PLUTO`

附加点位如 `CHIRON` / `NODE` 可作为参与成员，但：

- 默认不用于 chart shape
- 第一版不强制它们触发核心 pattern

### 7.4 相位边去重

所有用于 pattern detection 的相位边都必须按无向边去重。

例如：

- `SUN square MOON`
- `MOON square SUN`

只能保留一条。

### 7.5 Pattern 识别规则

#### T-square

满足：

- 3 个不同成员
- `1 opposition`
- `2 squares`

#### Grand Trine

满足：

- 3 个不同成员
- 任意两两之间都是 `trine`

#### Grand Cross

满足：

- 4 个不同成员
- `2 oppositions`
- `4 squares`

#### Kite

满足：

- 先识别出一个 `Grand Trine`
- 第 4 点与其中 1 点 `opposition`
- 与另外 2 点 `sextile`

#### Yod

满足：

- 3 个不同成员
- `1 sextile`
- 第 3 点分别与该 sextile 两端形成 `2 quincunx`

#### Mystic Rectangle

满足：

- 4 个不同成员
- `2 oppositions`
- `2 trines`
- `2 sextiles`

#### Stellium

满足任一：

- 同星座 `>= 3` 个主要点
- 同宫 `>= 3` 个主要点

输出时要区分：

- `stellium_sign`
- `stellium_house`

### 7.6 Orb 阈值

固定阈值：

- major pattern aspects：`6°`
- Yod 中 quincunx：`3°`
- quintile 家族先不纳入 pattern detection v1

### 7.7 输出字段

每个 pattern 至少返回：

- `id`
- `type`
- `members`
- `aspect_types`
- `orb_summary`
- `confidence`

### 7.8 Confidence 规则

项目 v1 简化规则：

- 若所有参与相位 orb 都小于阈值的 `40%`，则 `confidence = "high"`
- 否则 `confidence = "medium"`

## 8. 建议输出协议

### 8.1 Synastry

顶层字段建议：

- `meta`
- `person_a`
- `person_b`
- `person_a_angles`
- `person_b_angles`
- `person_a_houses`
- `person_b_houses`
- `person_a_planets`
- `person_b_planets`
- `cross_aspects`
- `a_in_b_houses`
- `b_in_a_houses`
- `patterns`
- `warnings`

### 8.2 Composite / Davison

顶层字段建议：

- `meta`
- `angles`
- `houses`
- `planets`
- `aspects`
- `patterns`
- `warnings`

### 8.3 Secondary Progressions

顶层字段建议：

- `meta`
- `natal_planets`
- `progressed_planets`
- `natal_angles`
- `progressed_angles`
- `natal_houses`
- `progressed_houses`
- `progressed_to_natal_aspects`
- `progressed_to_progressed_aspects`
- `progressed_lunation`
- `warnings`

### 8.4 Solar Arc

顶层字段建议：

- `meta`
- `natal_planets`
- `solar_arc_planets`
- `solar_arc_angles`
- `solar_arc_houses`
- `solar_arc_to_natal_aspects`
- `arc_value`
- `patterns`
- `warnings`

## 9. 必做测试

### 9.1 单元测试

必须至少覆盖：

- `circular_midpoint`
  - 普通情况
  - 跨 0° 情况
  - 对冲 180° 情况
- `secondary progression`
  - 1 岁 = 出生后 1 天
  - 30.5 岁 = 出生后 30.5 天
- `solar arc`
  - `arc == progressed_sun - natal_sun`
  - 所有点位都整体同幅度平移
- `synastry`
  - A/B 对调输入后 orb 不变
- `composite`
  - 中点跨 0° 正确
- `davison`
  - 时间中点与地点中点稳定
- `patterns`
  - 每种 pattern 至少一组正例
  - 每种 pattern 至少一组反例

### 9.2 Contract Tests

每个新 mode 都必须验证：

- 顶层字段完整
- list 字段始终是 list
- 空结果返回 `[]` 而不是 `null`
- `warnings` 始终是 list
- `meta.method` 存在且稳定

### 9.3 Sample Requests

新增：

- `Examples/sample-synastry-request.json`
- `Examples/sample-composite-request.json`
- `Examples/sample-davison-request.json`
- `Examples/sample-progressions-request.json`
- `Examples/sample-solar-arc-request.json`

## 10. 实现边界

这份规则专门服务 v1：

- 项目是计算器，不是解读器
- 不输出解释性文案
- 不做多流派切换
- `Composite` 固定 midpoint
- `Davison` 固定 midpoint time + arithmetic midpoint place
- `Secondary Progressions` 固定 day-for-year
- `Solar Arc` 固定 true solar arc
- `Pattern Detection` 固定按本文规则识别

不要让 agent 在这些地方“自由发挥”。
